---
title: EmptyState
description: A placeholder displayed when a section has no content. Includes an icon, title, description, and an optional call-to-action button.
---

import { EmptyStateDemo } from '@/components/examples/empty-state-demo';
import { ComponentPreview } from '@/components/component-preview';
import { PropsTable } from '@/components/props-table';

## Preview

<ComponentPreview name="EmptyState">
  <EmptyStateDemo />
</ComponentPreview>

## Installation

```tsx
import { EmptyState } from '@malix/ui';
```

## Usage

```tsx
import { EmptyState, Button } from '@malix/ui';
import { FolderOpen, Plus } from 'lucide-react';

export function ProjectsList() {
  return (
    <EmptyState
      icon={<FolderOpen size={24} />}
      title="No projects yet"
      description="Get started by creating your first project. You can organize your work and collaborate with your team."
      action={
        <Button
          hierarchy="primary"
          variant="leading-icon-text"
          icon={<Plus size={16} />}
        >
          Create Project
        </Button>
      }
    />
  );
}
```

## Props

<PropsTable
  props={[
    {
      name: 'icon',
      type: 'ReactNode',
      description: 'An icon displayed above the title.',
    },
    {
      name: 'title',
      type: 'string',
      required: true,
      description: 'The main heading of the empty state.',
    },
    {
      name: 'description',
      type: 'string',
      description: 'Supporting text displayed below the title.',
    },
    {
      name: 'action',
      type: 'ReactNode',
      description: 'A call-to-action element, typically a Button, displayed below the description.',
    },
  ]}
/>
