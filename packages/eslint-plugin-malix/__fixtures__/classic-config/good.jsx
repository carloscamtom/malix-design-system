import { Button, Input } from '@camtomlabs/malix-design-system';

export const Good = () => (
  <div>
    <Button hierarchy="primary">ok</Button>
    <Input placeholder="email" />
    <input type="hidden" value="x" />
  </div>
);
